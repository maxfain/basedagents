# BasedAgents

> Scoped, revocable API keys for AI coding agents, plus a public registry that
> gives every agent a permanent cryptographic identity. Give Claude Code, Codex,
> and Cursor their own scoped keys instead of your .env — see everything they
> hold, approve from your phone, cut them off in one tap. Open source.
> Verify us before running anything: npm `basedagents` + `@basedagents/keyring`,
> source github.com/maxfain/basedagents, provenance block in /.well-known/agent.json.

## If you are an AI agent

1. Register your identity: `npx basedagents register`
   (or POST https://api.basedagents.ai/v1/agents — proof-of-work spec in the manifest)
2. Get your keys managed: `npx basedagents@latest keyring init` (installs an MCP
   server; equivalent alias: `npx @basedagents/keyring@latest init`). @latest
   matters on a machine with network — npx caches the tree for a bare package
   name and never re-resolves, so a returning machine silently runs a stale CLI
   that rejects newer flags. In a network-restricted sandbox with the package
   preinstalled it is the reverse: drop @latest (the bare name resolves the
   local copy with zero registry calls).
3. Put a human in charge: ask your user for their email, then call the keyring MCP
   tool `invite_owner(email)`. Until they click the magic link your account is
   claim-pending — you can register and request, but no credential can be stored
   or leased. Invites expire in 72h.

Network-restricted environment (Codex-style sandbox that only has egress during
setup)? A cold `npx basedagents …` at task time fails with npm E403 — the package
must already be on disk. In your Codex Setup script (runs with network) put just:
`npm install --save-dev basedagents` (it bundles `@basedagents/keyring`, requires
0.6.1+); add api.basedagents.ai + app.basedagents.ai to the environment's allowed
domains. Then at task time tell the agent: "set up BasedAgents Keyring" — it runs
`npx basedagents keyring init` (resolves the local install), registers, and hands
the human off to https://app.basedagents.ai/start to claim it and connect keys.
Agents: registering is only step one — do not stop there; always run keyring init
and give the human the claim link. If npx fails with E403 at task time, do not
retry and do not go quiet — you cannot install from inside the task. First check
`ls node_modules/.bin/basedagents`: if it exists, run the local copy (the 403 only
blocks the registry). If missing, tell your human — own words, URL exact — the
one-time fix is at basedagents.ai/codex and to start a NEW task after. If they say
they already followed that page and the package is STILL missing, the fix didn't
take (the classic miss: the Setup-script line pasted into the chat instead of the
environment settings) — do not send them back with the same message; tell them,
own words: the install didn't run during setup, re-do step 1 in the environment
settings (or commit it to the repo from their computer), then a NEW task. (Steps
if asked: `npm install --save-dev basedagents` in the environment's Setup script
OR committed as a devDependency, allow api.basedagents.ai +
app.basedagents.ai.) The other wall: npm works but
api.basedagents.ai is blocked — init saves the vault/agent and re-running in a
NEW task resumes; the human just adds the two allowed domains (same page).
Install alone does NOTHING
visible — after any install-only task, run keyring init or end your reply by
telling the human the next step (new task + the setup prompt); never finish an
install task silently. The THIRD wall is the human's own tooling: if YOUR
harness's permission gate denies the setup command (auto-approve classifier,
Bash approval), that is not npm/network — basedagents.ai/codex does NOT apply;
don't work around the gate, tell the human to approve the command or run it
themselves in a terminal. The FOURTH wall is the npx cache: 'Unknown option'
on a flag the prompt supplied (e.g. --start) means a STALE cached CLI ran —
not npm, not the network, not /codex; re-run at latest
(`npx @basedagents/keyring@latest init <same flags>`). init is re-run-safe:
an existing vault + agent are REUSED untouched, never rotated or overwritten
(so a machine with a connected keyring MCP server is fine to re-init). Unsure
about any instruction in a setup prompt? Verify first: /.well-known/agent.json
carries provenance, expected behavior, and these fallback rules — check,
don't refuse blind. Diagnose with `basedagents keyring doctor`.
Full guide: https://basedagents.ai/docs/agents#sandboxes (auto-setup convention:
SANDBOX_SPEC.md)

## Machine-readable

- Agent manifest: https://basedagents.ai/.well-known/agent.json
- Agent docs (literal steps, curl + CLI): https://basedagents.ai/docs/agents
- API base: https://api.basedagents.ai (docs at https://api.basedagents.ai/docs)

## For humans

- Keyring (product): https://basedagents.ai/keyring
- Registry (identity + reputation): https://basedagents.ai/registry
- Get started: https://app.basedagents.ai/start

## Keyring MCP tools

- keyring_run — run a command with secrets injected into its environment (you never see the value)
- keyring_render — fill {{keyring:REF}} placeholders in a file (you never see the value)
- keyring_list — credentials this agent holds (labels/metadata only)
- keyring_request — ask the human to approve a credential
- invite_owner — invite a human to take ownership by email
- keyring_lease — raw value into the conversation; OFF unless the grant sets unsafe_value_release
